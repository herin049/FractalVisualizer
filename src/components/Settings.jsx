import React from 'react';

const Settings = () => {
    return (
        <div id="settings-overlay">
            <i className="fas fa-cogs" onClick={() => console.log('click')} />
        </div>
    );
};

export default Settings;
